# StaticForge Environment Configuration

# Site Information
SITE_NAME="My Static Site"
SITE_TAGLINE="Built with ❤️ and PHP"
SITE_BASE_URL="https://example.com"
TEMPLATE="sample"

# Directory Paths (relative to project root)
SOURCE_DIR="content"
OUTPUT_DIR="output"
TEMPLATE_DIR="templates"
FEATURES_DIR="src/Features"

# Optional Configuration
LOG_LEVEL="info"
LOG_FILE="staticforge.log"

# Chapter Navigation Feature
# Comma-separated list of menu numbers to build chapter navigation for
CHAPTER_NAV_MENUS="2"
# Symbol/text for previous links
CHAPTER_NAV_PREV_SYMBOL="←"
# Symbol/text for next links
CHAPTER_NAV_NEXT_SYMBOL="→"
# Separator between navigation elements
CHAPTER_NAV_SEPARATOR="|"CHAPTER_NAV_SEPARATOR="|"

# SFTP Upload Configuration
# Remote server hostname or IP address (required for site:upload)
SFTP_HOST="example.com"
# SFTP port (default: 22)
SFTP_PORT=22
# Username for authentication (required)
SFTP_USERNAME="your-username"
# Password authentication (optional - use password OR private key, not both)
SFTP_PASSWORD="your-password"
# Private key authentication (optional - recommended over password)
# Path to SSH private key file (e.g., /home/user/.ssh/id_rsa)
SFTP_PRIVATE_KEY_PATH=""
# Passphrase for encrypted private key (optional)
SFTP_PRIVATE_KEY_PASSPHRASE=""
# Remote directory path to upload site to (required, e.g., /var/www/html)
SFTP_REMOTE_PATH="/var/www/html"
