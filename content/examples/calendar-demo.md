---
title: Calendar Feature Demo
---
# Calendar Feature Demo

> **Note:** There is no built-in Calendar feature yet. The `calendars:` key in
> `siteconfig.yaml` is accepted but currently a no-op, and the shortcode below
> is not processed — it will render as literal text until a Calendar feature
> is built.

[[calendar name="example_events"]]
