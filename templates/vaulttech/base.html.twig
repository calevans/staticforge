<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ title | default('Untitled Page') }}{% if site_name %} | {{ site_name }}{% endif %}{% endblock %}</title>
    
    {% if description %}
    <meta name="description" content="{{ description }}">
    {% endif %}
{% if tags is defined and tags %}
{% if tags is iterable %}
{% if tags|length > 0 %}
    <meta name="keywords" content="{{ tags|join(', ') }}">
{% endif %}
{% else %}
    <meta name="keywords" content="{{ tags }}">
{% endif %}
{% endif %}
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Share+Tech+Mono:wght@400&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Orbitron', 'Arial', sans-serif;
            background: linear-gradient(135deg, #1a3c6e 0%, #2d5aa0 25%, #1a3c6e 50%, #0f1f3c 100%);
            color: #e8e6d3;
            line-height: 1.6;
            min-height: 100vh;
            background-image:
                radial-gradient(circle at 20% 20%, rgba(255, 205, 0, 0.1) 0%, transparent 25%),
                radial-gradient(circle at 80% 80%, rgba(255, 205, 0, 0.05) 0%, transparent 25%),
                repeating-linear-gradient(
                    90deg,
                    transparent 0px,
                    rgba(255, 205, 0, 0.03) 2px,
                    transparent 4px
                );
            animation: flicker 3s infinite alternate;
        }

        @keyframes flicker {
            0%, 100% { filter: brightness(1); }
            50% { filter: brightness(1.05); }
        }

        .vault-container {
            max-width: 1200px;
            margin: 20px auto;
            background: linear-gradient(135deg, #2c5282 0%, #1a365d 100%);
            border: 4px solid #ffcd00;
            border-radius: 20px;
            box-shadow:
                0 0 30px rgba(255, 205, 0, 0.4),
                inset 0 0 20px rgba(0, 0, 0, 0.3),
                0 0 0 8px #1a365d,
                0 0 0 12px #ffcd00;
            overflow: hidden;
            position: relative;
        }

        .vault-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background:
                repeating-linear-gradient(
                    0deg,
                    transparent 0px,
                    rgba(255, 205, 0, 0.05) 1px,
                    transparent 3px
                );
            pointer-events: none;
            z-index: 1;
        }

        .vault-header {
            background: linear-gradient(45deg, #ffcd00 0%, #ffd700 50%, #ffcd00 100%);
            color: #1a365d;
            padding: 20px;
            text-align: center;
            border-bottom: 4px solid #1a365d;
            position: relative;
            box-shadow: inset 0 0 20px rgba(0, 0, 0, 0.2);
        }

        .vault-header::before {
            content: 'VAULT-TEC';
            position: absolute;
            top: 5px;
            right: 20px;
            font-size: 12px;
            font-weight: 900;
            letter-spacing: 2px;
            opacity: 0.7;
        }

        .vault-logo {
            width: 60px;
            height: 60px;
            background: #1a365d;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 10px;
            font-size: 24px;
            font-weight: 900;
            color: #ffcd00;
            border: 3px solid #ffcd00;
            box-shadow: 0 0 10px rgba(255, 205, 0, 0.3);
        }

        .vault-body {
            padding: 30px;
            position: relative;
            z-index: 2;
        }

        header h1 {
            color: #ffcd00;
            font-size: 2.5rem;
            font-weight: 900;
            margin-bottom: 10px;
            text-shadow:
                0 0 10px rgba(255, 205, 0, 0.5),
                2px 2px 4px rgba(0, 0, 0, 0.3);
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        header p {
            color: #a0aec0;
            font-size: 1.1rem;
            margin-bottom: 20px;
            font-family: 'Share Tech Mono', monospace;
        }

        .terminal-display {
            background: #000;
            color: #00ff00;
            padding: 15px;
            border-radius: 8px;
            border: 2px solid #ffcd00;
            font-family: 'Share Tech Mono', monospace;
            font-size: 14px;
            margin: 20px 0;
            box-shadow:
                inset 0 0 10px rgba(255, 205, 0, 0.2),
                0 0 10px rgba(0, 0, 0, 0.5);
            position: relative;
        }

        .terminal-display::before {
            content: 'ROBCO INDUSTRIES (TM) TERMLINK PROTOCOL';
            position: absolute;
            top: -25px;
            left: 0;
            font-size: 10px;
            color: #ffcd00;
            background: #1a365d;
            padding: 2px 8px;
            border-radius: 4px 4px 0 0;
        }

        nav {
            margin: 30px 0;
            padding: 20px;
            background: rgba(0, 0, 0, 0.2);
            border-radius: 12px;
            border: 2px solid rgba(255, 205, 0, 0.3);
        }

        /* Generic menu styles - works anywhere */
        .menu {
            list-style: none;
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
        }

        .menu a {
            color: #e8e6d3;
            text-decoration: none;
            padding: 12px 20px;
            background: linear-gradient(135deg, #4a5568 0%, #2d3748 100%);
            border: 2px solid #ffcd00;
            border-radius: 8px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .menu a::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 205, 0, 0.2), transparent);
            transition: left 0.5s ease;
        }

        .menu a:hover::before {
            left: 100%;
        }

        .menu a:hover {
            background: linear-gradient(135deg, #ffcd00 0%, #ffd700 100%);
            color: #1a365d;
            transform: translateY(-2px);
            box-shadow:
                0 5px 15px rgba(255, 205, 0, 0.4),
                inset 0 0 10px rgba(0, 0, 0, 0.1);
        }

        /* Dropdown menu styles - generic, works in any .menu */
        .menu .dropdown {
            position: relative;
        }

        .menu .dropdown-title {
            color: #e8e6d3;
            padding: 12px 20px;
            background: linear-gradient(135deg, #4a5568 0%, #2d3748 100%);
            border: 2px solid #ffcd00;
            border-radius: 8px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            cursor: pointer;
            display: inline-block;
            transition: all 0.3s ease;
        }

        .menu .dropdown-title:hover {
            background: linear-gradient(135deg, #ffcd00 0%, #ffd700 100%);
            color: #1a365d;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 205, 0, 0.4);
        }

        .menu .dropdown-menu {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            background: #2d3748;
            border: 2px solid #ffcd00;
            border-radius: 8px;
            margin-top: 5px;
            min-width: 220px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.5);
            z-index: 1000;
        }

        .menu .dropdown:hover .dropdown-menu {
            display: block;
        }

        .menu .dropdown-menu li {
            display: block;
        }

        .menu .dropdown-menu a {
            display: block;
            border: none;
            border-bottom: 1px solid rgba(255, 205, 0, 0.2);
            border-radius: 0;
        }

        .menu .dropdown-menu li:last-child a {
            border-bottom: none;
        }

        /* Footer-specific menu positioning */
        footer .menu {
            margin-bottom: 20px;
        }

        main {
            margin: 30px 0;
            padding: 25px;
            background: rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            border: 1px solid rgba(255, 205, 0, 0.2);
        }

        .content h1, .content h2, .content h3 {
            color: #ffcd00;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
            text-shadow: 0 0 5px rgba(255, 205, 0, 0.3);
        }

        .content h1 {
            font-size: 2rem;
            border-bottom: 3px solid #ffcd00;
            padding-bottom: 10px;
            margin-bottom: 25px;
        }

        .content h2 {
            font-size: 1.5rem;
            color: #ffd700;
            margin-top: 30px;
        }

        .content p {
            color: #e8e6d3;
            margin-bottom: 18px;
            line-height: 1.8;
        }

        .content ul, .content ol {
            margin-left: 25px;
            margin-bottom: 18px;
            color: #e8e6d3;
        }

        .content li {
            margin-bottom: 8px;
        }

        .content code {
            background: rgba(255, 205, 0, 0.1);
            color: #ffcd00;
            padding: 3px 8px;
            border-radius: 4px;
            border: 1px solid rgba(255, 205, 0, 0.3);
            font-family: 'Share Tech Mono', monospace;
        }

        .btn {
            display: inline-block;
            background: linear-gradient(135deg, #ffcd00 0%, #ffd700 100%);
            color: #1a365d;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 8px;
            border: 2px solid #1a365d;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            box-shadow:
                0 5px 15px rgba(255, 205, 0, 0.3),
                inset 0 0 10px rgba(255, 255, 255, 0.1);
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn:hover {
            background: linear-gradient(135deg, #1a365d 0%, #2d5aa0 100%);
            color: #ffcd00;
            transform: translateY(-3px);
            box-shadow:
                0 8px 25px rgba(255, 205, 0, 0.4),
                inset 0 0 20px rgba(0, 0, 0, 0.2);
        }

        footer {
            padding: 25px;
            text-align: center;
            border-top: 2px solid rgba(255, 205, 0, 0.3);
            background: rgba(0, 0, 0, 0.2);
        }

        footer p {
            color: #a0aec0;
            font-size: 0.9rem;
            font-family: 'Share Tech Mono', monospace;
        }

        .pip-boy-display {
            background: #000;
            color: #00ff00;
            padding: 10px;
            border-radius: 4px;
            border: 1px solid #ffcd00;
            font-family: 'Share Tech Mono', monospace;
            font-size: 12px;
            margin: 10px 0;
            text-align: center;
        }

        .radiation-warning {
            color: #ff6b6b;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        @media (max-width: 768px) {
            .vault-container {
                margin: 10px;
                border-radius: 12px;
            }

            .vault-body {
                padding: 20px;
            }

            header h1 {
                font-size: 1.8rem;
            }

            nav ul {
                flex-direction: column;
                gap: 10px;
            }

            nav a {
                text-align: center;
            }
        }

        /* Vault door effect */
        .vault-container {
            position: relative;
        }

        .vault-container::after {
            content: '';
            position: absolute;
            top: 20px;
            right: 20px;
            width: 40px;
            height: 40px;
            background: #ffcd00;
            border-radius: 50%;
            border: 3px solid #1a365d;
            box-shadow:
                inset 0 0 10px rgba(0, 0, 0, 0.3),
                0 0 10px rgba(255, 205, 0, 0.5);
        }

        /* Category Files Grid - Vault-Tec Style */
        .category-files-section {
            margin: 30px 0;
        }

        .category-files-section h2 {
            color: #ffcd00;
            text-shadow: 0 0 10px rgba(255, 205, 0, 0.5);
            margin-bottom: 20px;
        }

        .category-files-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
            margin: 20px 0;
        }

        .category-file-item {
            background: rgba(26, 54, 93, 0.3);
            border: 2px solid rgba(255, 205, 0, 0.4);
            border-radius: 8px;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .category-file-item:hover {
            border-color: #ffcd00;
            box-shadow: 0 0 20px rgba(255, 205, 0, 0.4);
            transform: translateY(-3px);
        }

        .file-thumbnail {
            width: 100%;
            height: 200px;
            overflow: hidden;
            background: #000;
            border-bottom: 2px solid rgba(255, 205, 0, 0.3);
        }

        .file-thumbnail img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .category-file-item:hover .file-thumbnail img {
            transform: scale(1.05);
        }

        .file-info {
            padding: 15px;
        }

        .file-title {
            margin: 0 0 10px 0;
            font-size: 1.2rem;
        }

        .file-title a {
            color: #ffcd00;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .file-title a:hover {
            text-shadow: 0 0 10px rgba(255, 205, 0, 0.8);
        }

        .file-date {
            color: #b8b8b8;
            font-size: 0.9rem;
            margin-bottom: 10px;
        }

        .vault-label {
            color: #ffcd00;
            margin-right: 5px;
            font-family: 'Courier New', monospace;
        }

        .file-description {
            color: #d4d4d4;
            font-size: 0.95rem;
            line-height: 1.4;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .category-files-grid {
                grid-template-columns: 1fr;
            }
        }

        @media (min-width: 769px) and (max-width: 1024px) {
            .category-files-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }
    </style>
    {% block head %}{% endblock %}
</head>
<body>
    <div class="vault-container">
        <div class="vault-header">
            <div class="vault-logo">V</div>
            {% block site_title %}
            <h1>{{ site_name | default('VAULT-TEC INDUSTRIES') }}</h1>
            <p>{% block site_description %}BETTER LIVING UNDERGROUND{% endblock %}</p>
            {% endblock %}
        </div>

        <div class="vault-body">
            <div class="terminal-display">
                <div>{% block terminal_status %}VAULT STATUS: OPERATIONAL{% endblock %}</div>
                <div>OVERSEER ACCESS LEVEL: AUTHORIZED</div>
                <div>{% block terminal_message %}WELCOME TO THE FUTURE{% endblock %}</div>
            </div>

            <header>
                {% block header %}
                <h1>{% block page_title %}{{ title | default('Vault Database') }}{% endblock %}</h1>
                <p>{% block page_description %}Secure Information Terminal{% endblock %}</p>
                {% endblock %}
            </header>

            <nav>
                {% block navigation %}
                {% include 'menu1.html.twig' %}
                {% endblock %}
            </nav>

            <main>
                <div class="content">
                    {% block content %}
                    {{ content | raw }}
                    {% endblock %}
                </div>
            </main>

            <div class="pip-boy-display">
                {% block pip_boy %}
                VAULT-TEC © 2277 | RADIATION LEVEL: <span class="radiation-warning">SAFE</span> | POWER: 100%
                {% endblock %}
            </div>

            <footer>
                {% block footer %}
                {% include 'menu2.html.twig' %}
                <p>{% block copyright %}© {{ "now" | date("Y") }} {{ site_name | default('Vault-Tec Corporation') }}. War Never Changes.{% endblock %}</p>
                {% endblock %}
            </footer>
        </div>
    </div>

    {% block scripts %}{% endblock %}
</body>
</html>