{% extends "vaulttech/base.html.twig" %}

{% block title %}{{ title | default('Welcome to Vault-Tec') }}{% endblock %}

{% block terminal_status %}VAULT INITIALIZATION: COMPLETE{% endblock %}
{% block terminal_message %}ENTERING STATISTICAL ANALYSIS MODE{% endblock %}

{% block page_title %}{{ title | default('VAULT DATABASE ACCESS') }}{% endblock %}
{% block page_description %}Authorized Personnel Only{% endblock %}

{% block content %}
<div class="vault-interface">
    {% if features.CategoryIndex.category_files is defined and features.CategoryIndex.category_files|length > 0 %}
        <div class="category-files-section">
            <h2>📁 VAULT DATABASE RECORDS</h2>
            <div class="category-files-grid">
                {% for file in features.CategoryIndex.category_files %}
                    {% include 'vaulttech/partials/category-file-item.html.twig' with {'file': file} %}
                {% endfor %}
            </div>
        </div>
    {% else %}
    <div class="system-status">
        <h2>🏗️ VAULT CONSTRUCTION STATUS</h2>
        <div class="status-grid">
            <div class="status-item">
                <span class="status-label">SHELTER INTEGRITY:</span>
                <span class="status-value">100%</span>
            </div>
            <div class="status-item">
                <span class="status-label">LIFE SUPPORT:</span>
                <span class="status-value">OPTIMAL</span>
            </div>
            <div class="status-item">
                <span class="status-label">POPULATION:</span>
                <span class="status-value">GROWING</span>
            </div>
            <div class="status-item">
                <span class="status-label">MORALE:</span>
                <span class="status-value">HIGH</span>
            </div>
        </div>
    </div>

    <div class="content-data">
        {{ content | raw }}
    </div>

    <div class="vault-features">
        <h2>🔬 VAULT-TEC RESEARCH DIVISION</h2>
        <div class="research-grid">
            <div class="research-card">
                <h3>⚡ ATOMIC POWER</h3>
                <p>Clean, limitless energy from the atom itself. Powers our underground civilization for centuries.</p>
                <div class="tech-level">TECH LEVEL: SUPERIOR</div>
            </div>
            <div class="research-card">
                <h3>🏠 UNDERGROUND LIVING</h3>
                <p>State-of-the-art subterranean habitats designed for comfort and safety in the post-atomic world.</p>
                <div class="tech-level">TECH LEVEL: ADVANCED</div>
            </div>
            <div class="research-card">
                <h3>🤖 ROBOTIC ASSISTANCE</h3>
                <p>Helpful robot companions handle maintenance, security, and daily tasks for vault residents.</p>
                <div class="tech-level">TECH LEVEL: EXPERIMENTAL</div>
            </div>
            <div class="research-card">
                <h3>🧬 GENETIC PRESERVATION</h3>
                <p>Cutting-edge preservation technology ensures the continuation of human civilization.</p>
                <div class="tech-level">TECH LEVEL: CLASSIFIED</div>
            </div>
        </div>
    </div>

    <div class="pip-boy-section">
        <h2>📱 PIP-BOY INTEGRATION</h2>
        <div class="pip-interface">
            <div class="pip-stats">
                <div class="stat-row">
                    <span>STRENGTH:</span>
                    <div class="stat-bar"><div class="stat-fill" style="width: 85%"></div></div>
                    <span>8</span>
                </div>
                <div class="stat-row">
                    <span>PERCEPTION:</span>
                    <div class="stat-bar"><div class="stat-fill" style="width: 70%"></div></div>
                    <span>7</span>
                </div>
                <div class="stat-row">
                    <span>ENDURANCE:</span>
                    <div class="stat-bar"><div class="stat-fill" style="width: 90%"></div></div>
                    <span>9</span>
                </div>
                <div class="stat-row">
                    <span>CHARISMA:</span>
                    <div class="stat-bar"><div class="stat-fill" style="width: 60%"></div></div>
                    <span>6</span>
                </div>
            </div>
        </div>
    </div>

    <div class="mission-briefing">
        <h2>📋 MISSION BRIEFING</h2>
        <div class="briefing-content">
            <p><strong>OBJECTIVE:</strong> Explore the capabilities of StaticForge using Vault-Tec's revolutionary technology.</p>
            <p><strong>EQUIPMENT:</strong> PHP 8.4+, Twig templating engine, and pre-war knowledge.</p>
            <p><strong>EXPECTED OUTCOME:</strong> Successfully deploy static websites that can survive nuclear fallout.</p>

            <div class="action-items">
                <h3>RECOMMENDED ACTIONS:</h3>
                <ul>
                    <li>Modify content files in the /content/ directory</li>
                    <li>Customize Vault-Tec approved templates</li>
                    <li>Execute site generation protocols</li>
                    <li>Deploy to the surface world (when safe)</li>
                </ul>
            </div>
        </div>
    </div>

    <div class="emergency-protocols">
        <h2 class="radiation-warning">☢️ EMERGENCY PROTOCOLS</h2>
        <div class="protocol-box">
            <p><strong>IN CASE OF RADIATION LEAK:</strong></p>
            <p>1. Remain calm and follow vault procedures</p>
            <p>2. Report to your designated Radaway station</p>
            <p>3. Contact the Overseer immediately</p>
            <p>4. Remember: War Never Changes, but websites do!</p>
        </div>
    </div>
    {% endif %}
</div>

<style>
    .vault-interface {
        font-family: 'Orbitron', sans-serif;
    }

    .system-status {
        background: rgba(0, 0, 0, 0.3);
        padding: 20px;
        border-radius: 8px;
        border: 2px solid #ffcd00;
        margin-bottom: 30px;
    }

    .status-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 15px;
        margin-top: 15px;
    }

    .status-item {
        display: flex;
        justify-content: space-between;
        background: rgba(255, 205, 0, 0.1);
        padding: 10px;
        border-radius: 4px;
        border: 1px solid rgba(255, 205, 0, 0.3);
    }

    .status-label {
        color: #e8e6d3;
        font-size: 12px;
    }

    .status-value {
        color: #00ff00;
        font-weight: bold;
        font-family: 'Share Tech Mono', monospace;
    }

    .content-data {
        margin: 30px 0;
        padding: 20px;
        background: rgba(255, 205, 0, 0.05);
        border-radius: 8px;
        border: 1px solid rgba(255, 205, 0, 0.2);
    }

    .vault-features {
        margin: 30px 0;
    }

    .research-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-top: 20px;
    }

    .research-card {
        background: linear-gradient(135deg, #2d5aa0 0%, #1a3c6e 100%);
        padding: 20px;
        border-radius: 8px;
        border: 2px solid #ffcd00;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        transition: transform 0.3s ease;
    }

    .research-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px rgba(255, 205, 0, 0.2);
    }

    .research-card h3 {
        color: #ffcd00;
        margin-bottom: 10px;
        font-size: 1.1rem;
    }

    .research-card p {
        color: #e8e6d3;
        font-size: 0.9rem;
        line-height: 1.6;
        margin-bottom: 10px;
    }

    .tech-level {
        color: #00ff00;
        font-family: 'Share Tech Mono', monospace;
        font-size: 0.8rem;
        font-weight: bold;
        text-align: right;
    }

    .pip-boy-section {
        margin: 30px 0;
        background: rgba(0, 0, 0, 0.4);
        padding: 20px;
        border-radius: 8px;
        border: 2px solid #ffcd00;
    }

    .pip-interface {
        margin-top: 15px;
    }

    .pip-stats {
        max-width: 400px;
    }

    .stat-row {
        display: flex;
        align-items: center;
        margin-bottom: 10px;
        gap: 10px;
        color: #e8e6d3;
        font-family: 'Share Tech Mono', monospace;
        font-size: 0.9rem;
    }

    .stat-row span:first-child {
        min-width: 100px;
    }

    .stat-bar {
        flex: 1;
        height: 12px;
        background: rgba(0, 0, 0, 0.5);
        border-radius: 6px;
        border: 1px solid #ffcd00;
        overflow: hidden;
    }

    .stat-fill {
        height: 100%;
        background: linear-gradient(90deg, #ffcd00 0%, #ffd700 100%);
        transition: width 0.3s ease;
    }

    .stat-row span:last-child {
        min-width: 20px;
        text-align: center;
        color: #00ff00;
        font-weight: bold;
    }

    .mission-briefing {
        margin: 30px 0;
        background: rgba(0, 0, 0, 0.2);
        padding: 20px;
        border-radius: 8px;
        border: 2px solid rgba(255, 205, 0, 0.5);
    }

    .briefing-content p {
        margin-bottom: 15px;
        color: #e8e6d3;
    }

    .action-items {
        margin-top: 20px;
        padding: 15px;
        background: rgba(255, 205, 0, 0.1);
        border-radius: 4px;
        border: 1px solid rgba(255, 205, 0, 0.3);
    }

    .action-items h3 {
        color: #ffcd00;
        margin-bottom: 10px;
        font-size: 1rem;
    }

    .action-items ul {
        color: #e8e6d3;
    }

    .emergency-protocols {
        margin: 30px 0;
        background: rgba(255, 0, 0, 0.1);
        padding: 20px;
        border-radius: 8px;
        border: 2px solid #ff6b6b;
        animation: pulse 3s infinite;
    }

    .protocol-box {
        background: rgba(0, 0, 0, 0.3);
        padding: 15px;
        border-radius: 4px;
        border: 1px solid #ff6b6b;
        margin-top: 10px;
    }

    .protocol-box p {
        color: #e8e6d3;
        margin-bottom: 8px;
        font-family: 'Share Tech Mono', monospace;
        font-size: 0.9rem;
    }

    @media (max-width: 768px) {
        .status-grid {
            grid-template-columns: 1fr;
        }

        .research-grid {
            grid-template-columns: 1fr;
        }

        .stat-row {
            font-size: 0.8rem;
        }

        .stat-row span:first-child {
            min-width: 80px;
        }
    }
</style>
{% endblock %}

{% block pip_boy %}
VAULT-TEC © 2287 | SITE STATUS: <span style="color: #00ff00;">ONLINE</span> | OVERSEER: PRESENT
{% endblock %}